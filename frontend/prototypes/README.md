# Цветочный B2B - HTML Prototypes (Phase 1)

Статические HTML прототипы для согласования дизайна и UX перед разработкой production frontend.

## 📁 Структура

```
frontend/prototypes/
├── index.html          # Витрина (каталог товаров)
├── buyer.html          # Кабинет покупателя (корзина + заказы)
├── seller.html         # Кабинет продавца (загрузка прайсов + управление заказами)
├── assets/
│   └── mock-data.js    # Тестовые данные
└── README.md           # Этот файл
```

## 🚀 Как запустить

### Вариант 1: VS Code Live Server (рекомендуется)

1. Установите расширение "Live Server" в VS Code
2. Откройте `index.html`
3. Кликните правой кнопкой → "Open with Live Server"
4. Откроется браузер на `http://localhost:5500/index.html`

### Вариант 2: Python HTTP Server

```bash
cd frontend/prototypes
python -m http.server 5500
```

Откройте: `http://localhost:5500/index.html`

### Вариант 3: Node.js http-server

```bash
cd frontend/prototypes
npx http-server -p 5500
```

Откройте: `http://localhost:5500/index.html`

## 📄 Страницы

### 1. index.html - Витрина

**URL**: `http://localhost:5500/index.html`

**Функционал**:
- ✅ Hero section с градиентом
- ✅ Поиск по названию, сорту, поставщику
- ✅ Фильтры по типу цветов (розы, тюльпаны, пионы и т.д.)
- ✅ Сортировка (цена, название)
- ✅ Сетка товаров (4 колонки на desktop)
- ✅ Карточки товаров с ценой, остатком, расстоянием
- ✅ Добавление в корзину (localStorage)
- ✅ Badge корзины в header (обновляется динамически)

**Интерактивность**:
- Клик на фильтр (тип цветов) → фильтрация товаров
- Поиск → debounced фильтрация (через input event)
- Сортировка → пересортировка товаров
- Кнопка "+"/"-" → изменение количества
- "В корзину" → добавление в localStorage + alert

### 2. buyer.html - Кабинет покупателя

**URL**: `http://localhost:5500/buyer.html`

**Функционал**:
- ✅ Корзина с группировкой по поставщикам
- ✅ Управление количеством (+/- кнопки)
- ✅ Удаление товаров из корзины
- ✅ Расчет итоговой суммы по каждой базе
- ✅ Кнопка "Оформить заказ" (demo alert)
- ✅ История заказов (hardcoded из mock-data.js)
- ✅ Детали заказа с timeline доставки (раскрываемый блок)
- ✅ Статистика (общая сумма, экономия, кол-во заказов)

**Интерактивность**:
- "+"/"-" → изменение количества в корзине
- "✕" → удаление товара
- "Оформить заказ" → alert + очистка корзины для этого поставщика
- `<details>` → раскрытие timeline доставки

### 3. seller.html - Кабинет продавца

**URL**: `http://localhost:5500/seller.html`

**Функционал**:
- ✅ Drag-and-drop зона для загрузки прайс-листа (XLSX/CSV/TXT)
- ✅ Статистика (товаров в каталоге, требуют обработки, активных заказов)
- ✅ Редактор ошибок нормализации (dropdown для выбора SKU)
- ✅ Быстрое обновление остатков (input + кнопка "Обновить")
- ✅ Таблица входящих заказов
- ✅ Фильтры по статусу (Все, Ожидают, Подтверждены, Отклонены)
- ✅ Кнопки "Подтвердить"/"Отклонить" заказ

**Интерактивность**:
- Клик на dropzone → выбор файла (file input)
- "Загрузить прайс-лист" → alert (demo)
- Dropdown SKU + "Применить" → alert (demo)
- "Обновить" остаток → alert (demo)
- Табы статусов → переключение активной вкладки
- "Подтвердить" → confirm + alert
- "Отклонить" → prompt (причина) + alert

## 🎨 Дизайн

**Цветовая схема**:
- Primary: Indigo (#6366F1) → Purple (#8B5CF6) gradient
- Success: Green (#10B981)
- Warning: Orange (#F59E0B)
- Danger: Red (#EF4444)
- Gray scale: 50, 100, 200, ..., 800

**Typography**:
- Font: System fonts (sans-serif)
- Заголовки: Bold, 2xl-4xl
- Текст: Regular, sm-base

**Components**:
- Buttons: rounded-lg, px-4-8, py-2-3
- Cards: rounded-lg, shadow-sm
- Inputs: border, rounded-lg, focus:ring-2
- Badges: rounded-full, px-3, py-1

## 📱 Responsive

- **Mobile** (320px - 640px): 1 колонка
- **Tablet** (641px - 1024px): 2 колонки
- **Desktop** (1025px+): 4 колонки

Tailwind breakpoints: `sm:`, `md:`, `lg:`

## 💾 Данные

Все данные загружаются из `assets/mock-data.js`:

- `MOCK_DATA.products` - 13 товаров (розы, тюльпаны, пионы и т.д.)
- `MOCK_DATA.cart` - корзина (2 поставщика, 3 товара)
- `MOCK_DATA.orders` - история заказов (3 заказа)
- `MOCK_DATA.sellerStats` - статистика продавца
- `MOCK_DATA.normalizationErrors` - ошибки нормализации (2 товара)
- `MOCK_DATA.incomingOrders` - входящие заказы (3 заказа)

**Helper функции**:
- `MockDataHelpers.filterProducts(filters)` - фильтрация
- `MockDataHelpers.sortProducts(products, sortBy)` - сортировка
- `MockDataHelpers.calculateCartTotal(supplier)` - расчет суммы

## 🔧 localStorage API

**Корзина** хранится в `localStorage.cart`:

```js
[
  {
    "supplier_id": "s1",
    "supplier_name": "База №1",
    "items": [
      {
        "product_id": "1",
        "name": "Роза Красная 40 см",
        "price": 120,
        "quantity": 50,
        "stock": 180
      }
    ]
  }
]
```

**API**:
- `localStorage.getItem('cart')` - получить корзину
- `localStorage.setItem('cart', JSON.stringify(cart))` - сохранить
- Автообновление badge в header при изменениях

## ✅ Функционал (DoD Phase 1)

- [x] 3 HTML страницы созданы
- [x] Соответствуют референсам (layout, цвета, spacing)
- [x] Все интерактивные элементы работают
- [x] Responsive на mobile/tablet/desktop
- [x] Mock данные загружаются корректно
- [x] Корзина сохраняется в localStorage
- [x] Tailwind CSS подключен (CDN)
- [x] Нет внешних зависимостей (кроме Tailwind CDN)

## 🐛 Известные ограничения (MVP)

- Изображения товаров = emoji (нет реальных фото)
- Нет реальной интеграции с backend API
- Кнопки "Войти", "Регистрация" не работают (нет авторизации)
- "Оформить заказ" → alert (не создает реальный заказ)
- Загрузка файлов → alert (не парсит файл)
- Все действия продавца → alert (не сохраняются)

**Это нормально для Phase 1** - цель: согласовать дизайн/UX перед Phase 2.

## 🚀 Следующий шаг: Phase 2

После согласования прототипов:
1. React + TypeScript + Vite
2. Интеграция с API (Tasks 1-3)
3. Redux + RTK Query
4. React Router
5. Настоящая авторизация (Task 4)
6. Production build + deploy

См. `docs/Task 9.md` для полной спецификации Phase 2.

## 📞 Контакты

Вопросы по прототипам → см. `docs/Task 9.md`

---

**Создано**: 2025-01-13
**Версия**: Phase 1 (HTML Prototypes)
**Статус**: ✅ Готово к ревью
